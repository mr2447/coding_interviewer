FROM python:3.12-slim

#Install AWS SDK
RUN pip install boto3

WORKDIR /app

#Install compilers
RUN apt-get update && apt-get install -y \
    g++ gcc \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

COPY cpp_code_runner.py .

ENTRYPOINT ["python3", "cpp_code_runner.py"]